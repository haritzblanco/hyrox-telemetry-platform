# Valores de la plataforma. Cada clave configura el subchart del mismo nombre;
# los defaults de cada componente están en charts/<componente>/values.yaml.

influxdb:
  # Credenciales de desarrollo. En un despliegue real irían en un gestor de
  # secretos externo, no en el repositorio.
  auth:
    username: admin
    password: haritzblanco
    org: hyrox
    bucket: telemetry
    token: token1234567890

grafana:
  adminUser: admin
  adminPassword: haritzblanco
  influx:
    token: token1234567890

processor:
  image: hyrox/processor:0.2.0
