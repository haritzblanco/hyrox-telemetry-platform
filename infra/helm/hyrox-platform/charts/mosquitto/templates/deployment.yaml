apiVersion: apps/v1
kind: Deployment
metadata:
  name: mosquitto
  labels:
    app.kubernetes.io/name: mosquitto
    app.kubernetes.io/component: broker
    app.kubernetes.io/part-of: hyrox-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mosquitto
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mosquitto
        app.kubernetes.io/component: broker
        app.kubernetes.io/part-of: hyrox-platform
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        # Prometheus raspa el caudal que expone el sidecar exporter en /metrics.
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.exporter.port | quote }}
        prometheus.io/path: /metrics
    spec:
      # Solo el sidecar exporter se descarga del registro privado (DOCR); la
      # imagen de mosquitto viene de Docker Hub y no necesita credenciales.
      imagePullSecrets:
        - name: registry-haritzblanco
      containers:
        - name: mosquitto
          image: {{ .Values.image }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: mqtt
              containerPort: 1883
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /mosquitto/config/mosquitto.conf
              subPath: mosquitto.conf
              readOnly: true
            - name: data
              mountPath: /mosquitto/data
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: 1883
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 1883
            initialDelaySeconds: 2
            periodSeconds: 5
        # Exporter de carga como sidecar: se suscribe a $SYS por localhost y
        # sirve el caudal de publicaciones en JSON. KEDA lo consulta para
        # escalar el procesador por carga ofrecida en vez de por CPU.
        - name: exporter
          image: {{ .Values.exporter.image }}
          imagePullPolicy: IfNotPresent
          args:
            - --broker-host
            - localhost
            - --http-port
            - {{ .Values.exporter.port | quote }}
          ports:
            - name: metrics
              containerPort: {{ .Values.exporter.port }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 2
            periodSeconds: 10
          resources:
            {{- toYaml .Values.exporter.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: mosquitto-config
        - name: data
          emptyDir: {}
